{
  "name": "viewer-merusuh-client",
  "version": "0.5.0",
  "description": "Client agent untuk Viewer Merusuh — menjalankan efek di PC Gaming",
  "main": "src/index.js",
  "scripts": {
    "start":        "node src/index.js",
    "dev":          "nodemon src/index.js",
    "setup":        "node scripts/setup.js",
    "test:vjoy":    "node scripts/test-vjoy.js",
    "test:plugin":  "node scripts/test-plugin.js",
    "dashboard":    "node -e \"require('dotenv').config(); const d=require('./src/core/dashboard'); const am=require('./src/core/adapterManager'); d.init(am,null).then(()=>console.log('Dashboard only mode'))\""
  },
  "dependencies": {
    "dotenv":           "^16.0.0",
    "socket.io-client": "^4.7.0"
  },
  "optionalDependencies": {
    "vigemclient": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
